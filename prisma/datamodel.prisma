type User {
  id: ID! @unique
  createdAt: DateTime!
  email: String! @unique
  salt: String!
  password: String!
  name: String!
  roles: [Role]
}

type Role {
  id: ID! @unique
  createdAt: DateTime!
  permissions: String
  name: String!
  attachedTo: [User]
}